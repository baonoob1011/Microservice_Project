jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - common
          - ApiGateway
          - CartService
          - ConfigServer
          - EurekaService   # sửa chính tả
          - NotificationService
          - ProductService
          - ProfileService
          - UserService

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build and install Common only
      - name: Build and install Common
        if: matrix.service == 'common'
        run: |
          cd common
          mvn clean install -DskipTests

      # Build and push Docker images only for services that have Dockerfile
      - name: Build and Push Docker image
        if: matrix.service != 'common'
        run: |
          cd "${{ matrix.service }}"
          mvn clean package -DskipTests
          IMAGE_NAME=$(echo ${{ matrix.service }} | tr '[:upper:]' '[:lower:]')
          docker build -t ${{ secrets.DOCKER_USERNAME }}/${IMAGE_NAME}:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/${IMAGE_NAME}:latest
